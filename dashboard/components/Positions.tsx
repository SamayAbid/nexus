import type { Position } from '@/lib/types'
export function Positions({ positions }: { positions: Position[] }) {
  return <div className="panel positions-panel"><div className="panel-header"><span className="panel-title">OPEN POSITIONS</span></div></div>
}
