'use client'
import type { SignalItem } from '@/lib/types'
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Signals(_: { signals: SignalItem[] }) {
  return <div style={{ color: 'var(--text-2)', padding: 20 }}>Signals — coming soon</div>
}
