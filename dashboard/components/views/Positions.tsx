'use client'
import type { Position } from '@/lib/types'
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Positions(_: { positions: Position[] }) {
  return <div style={{ color: 'var(--text-2)', padding: 20 }}>Positions — coming soon</div>
}
