'use client'
import type { StatusData, PnLData } from '@/lib/types'
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Risk(_: { status: StatusData; pnl: PnLData }) {
  return <div style={{ color: 'var(--text-2)', padding: 20 }}>Risk — coming soon</div>
}
