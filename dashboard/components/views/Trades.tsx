'use client'
import type { TradesData } from '@/lib/types'
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Trades(_: { trades: TradesData }) {
  return <div style={{ color: 'var(--text-2)', padding: 20 }}>Trades — coming soon</div>
}
