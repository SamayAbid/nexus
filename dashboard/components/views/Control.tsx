'use client'
import type { StatusData } from '@/lib/types'
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Control(_: { status: StatusData; onChanged: () => void }) {
  return <div style={{ color: 'var(--text-2)', padding: 20 }}>Control — coming soon</div>
}
